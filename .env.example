# Database Configuration
DB_NAME=
DB_USER=
DB_URL=jdbc:postgresql://database:5432/${DB_NAME}
# Password is stored in ./secrets/db_password.txt

# Content moderation (set to false to disable)
MODERATION_ENABLED=true